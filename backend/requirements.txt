# 智修Agent 后端依赖

# FastAPI 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# 基础依赖（复用项目根目录的依赖）
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# 注意：需要同时安装项目根目录的依赖
# pip install -r ../requirements.txt
